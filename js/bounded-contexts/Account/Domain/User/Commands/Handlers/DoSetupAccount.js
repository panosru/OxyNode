define([
	
],

function (
	
) {
	var DoSetupAccountCommandHandler = function () {
		//Command Handler is responsible of transfer client data to server
		log('handle the command!');
	};
	
	return DoSetupAccountCommandHandler;
});
